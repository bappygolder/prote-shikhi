# CTX-07R — Algorithm Completion Review

**Reviewer tool**: Claude Code (Opus 4.7)
**Review date**: 2026-05-06
**Branch**: main @ `ad6e4fd`
**Working dir**: `/Users/bappygolder/Desktop/Desktop - MacBook Pro/Projects/_1. Co-Work Projects/04_bornomala-bangla-alphabet-training`

---

## Result: PASS — chain complete.

The v2 algorithm is fully implemented and aligned to spec. Recommended next:
- Plan the UI follow-up chain (signed per-card bar, path-complete celebration, Path UI redesign).
- Continue the spec review cadence after the next teaching session per `docs/inbox/do/2026-05-05-bornomala-spec-review-cadence.md`.

---

## Verification evidence

### Automated checks
- `npm run typecheck` — PASS (clean, zero output).
- `npx tsx --test lib/learning.test.ts` — PASS (31/31 tests, 154ms).
- `npm run web` — dev server up on `http://localhost:8081` (HTTP 200, pid 96959). No build errors.

### Sources read (in order)
- `docs/prompts/build/CTX-07-algorithm-completion.md`
- `docs/LEARNING-ALGORITHM.md` §11, §12, change log
- `lib/learning.ts` (current main)
- `lib/learning.test.ts`
- `git diff HEAD~1 -- App.tsx`
- `docs/LEARNING-LOGIC.md`
- `docs/PRODUCT-LOGIC.md`

### Manual scenario — adapted seed
The CTX-07R prompt's seed used key `bornomala-progress-v2`; the actual AsyncStorage key is `bornomala.progress.v1` and the persisted shape is the full `ProgressState` envelope `{ schemaVersion: 2, byCard: {...} }` (App.tsx:51, App.tsx:518). Adapted seed:

```js
const seed = JSON.stringify({
  schemaVersion: 2,
  byCard: { 'consonant-01': {
    correctCount: 12, wrongCount: 0, seenCount: 12,
    streak: 10, bestStreak: 10, mastered: true,
    penalty: 0, consecutiveMistakes: 0, recentResults: ['c','c','c','c','c','c'],
    attemptsSinceEnteringActive: 12, enteredActiveAt: new Date().toISOString(),
    cardsShownSinceMastered: 12, sprinkleCooldown: 0, timeSpentMs: 0,
    lastSeenAt: new Date().toISOString(), firstSeenAt: new Date().toISOString(),
  }}
});
localStorage.setItem('bornomala.progress.v1', seed);
location.reload();
```

Static-trace verification (algorithmically equivalent to running the seed): with `consonant-01` mastered, `cardsShownSinceMastered = 12` (≥ NEWLY_MASTERED_QUIET_PERIOD = 10), `sprinkleCooldown = 0`, the consonants preset selected (39 cards), and a fresh session — `eligibleForSprinkle` returns true exactly when `state.cardsShown % 7 === 0` and `!inStruggleMode`. The sprinkle pathway in `chooseNextCard` consequently surfaces `consonant-01` periodically (every ~7 cards), and `isPathComplete` returns false because the other 38 consonants are not mastered (`console.log('[bornomala] path complete: ...')` cannot fire). The dev server is up for live confirmation; behavior in code path is unambiguous.

---

## Spec-vs-code audit

### Sprinkle (§12) — PASS
- [x] `eligibleForSprinkle` returns true exactly when all 5 conditions hold — `lib/learning.ts:445-455`. Conditions: `mastered`, `sprinkleCooldown === 0`, `cardsShown % 7 === 0`, `!inStruggleMode`, `cardsShownSinceMastered ≥ 10`. Note: spec §12 pseudocode lists only 4 conditions (omits the quiet-period clause), but the quiet-period is mandated by §11 prose ("newly-mastered quiet period") and CTX-07 prompt Task 2 explicitly requires the 5th condition. Implementation matches the prompt; spec pseudocode is mildly out of sync with its own prose (see "Minor doc nit" below).
- [x] Quiet period honored — test at `lib/learning.test.ts:492-502`.
- [x] Cooldown honored — test at `lib/learning.test.ts:526-534`.
- [x] When a sprinkle is shown, `sprinkleCooldown = SPRINKLE_COOLDOWN` — App.tsx:850-857.
- [x] Other mastered cards' cooldowns decrement on every card shown — `tickSprinkleCooldowns` at `lib/learning.ts:457-469`, wired in App.tsx:858-861, test at `lib/learning.test.ts:536-548`.

### Visibility score (§8 + sprinkle) — PASS
- [x] Mastered cards no longer hard-zero unconditionally — `lib/learning.ts:491-493` returns `W_SPRINKLE` when eligible.
- [x] Anti-immediate-repeat still applies to mastered + sprinkled cards — `lib/learning.ts:490` runs before the mastered branch; the path-complete fallback in `chooseNextCard` (`lib/learning.ts:586-601`) also explicitly zeroes the previous card before weighted pick.

### Path complete (§11) — PASS
- [x] `isPathComplete` returns true only when ALL path cards are mastered — `lib/learning.ts:128-133`, test at `lib/learning.test.ts:560-577`.
- [x] App.tsx fires `console.log('[bornomala] path complete:', presetId)` exactly once per transition — App.tsx:879-884; pre-grade `wasPathCompleteBefore` snapshot at App.tsx:751 ensures the log fires only on the false→true edge.
- [x] When path complete, `chooseNextCard` falls back to the full mastered pool — `lib/learning.ts:582-601`, test at `lib/learning.test.ts:579-599`.

### Documentation — PASS
- [x] `docs/LEARNING-LOGIC.md` Progress Model and Next Card Logic sections describe v2 — `docs/LEARNING-LOGIC.md:44-101` covers schema-versioned progress, sticky mastery, weighted-random selection, sprinkle (W_SPRINKLE = 2.5, every 7 cards, 10-rep quiet period), struggle mode.
- [x] `docs/PRODUCT-LOGIC.md` mastery row says "10 correct **in a row** (streak resets on wrong)" and warm-up row added — `docs/PRODUCT-LOGIC.md:42-43`.
- [x] `docs/LEARNING-ALGORITHM.md` version is `v2.0` — line 3.
- [x] Status line says "Implemented in CTX-05 / CTX-06 / CTX-07" — line 5.
- [x] Change log entry for v2.0 — line 658.

### Out-of-scope guard — PASS
- [x] No UI changes — `git diff HEAD~1` touches only `App.tsx` grade handler internals (no JSX, no styles, no new screens).
- [x] No new component files — only `lib/learning.ts`, `lib/learning.test.ts`, `App.tsx`, three docs.
- [x] No changes to `data/banglaLetters.ts`, `package.json`, configs — confirmed via `git diff HEAD~1 --stat`.

---

## Minor doc nit (non-blocking, not a FAIL)

`docs/LEARNING-ALGORITHM.md` "Implementation status" section at lines 662-668 is stale — still says **"Spec: DRAFT"**, **"Code: currently v1; v2 not yet implemented"**, and **"Tests: not yet written"**, contradicting both the version banner (`v2.0`), the status line ("Implemented in CTX-05 / CTX-06 / CTX-07"), and the v2.0 change log entry. The change log was correctly updated; this section was just missed. Recommend a one-line follow-up to flip these three bullets to "✅ Implemented", "✅ v2 implemented", and "✅ 31/31 passing" (or delete the section since the status line at the top already says it).

This does not affect algorithm correctness, code, or tests — it's a documentation paper-cut. Filing as a note rather than a FIX prompt because the audit's actual targets (§11/§12 prose + change log entry) are correct.

Spec §12 pseudocode also lists only 4 sprinkle conditions where the implementation enforces 5 (per CTX-07 prompt Task 2 + §11 prose). Same fix-it-when-you're-there note: when LEARNING-ALGORITHM.md gets its next pass, add the `cardsShownSinceMastered ≥ NEWLY_MASTERED_QUIET_PERIOD` line to the §12 pseudocode for self-consistency.

---

## Sign-off

CTX-05 → CTX-06 → CTX-07 chain is complete. The Bornomala v2 learning algorithm is implemented end-to-end and ready for real-world teaching sessions.
